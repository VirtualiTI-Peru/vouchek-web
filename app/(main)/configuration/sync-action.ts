"use server";

export async function syncCustomersServerAction() {
  'use server';
  return { success: false, message: 'La sincronización de empresas ya no está disponible. Gestiona las empresas directamente desde Configuración.' };
}
