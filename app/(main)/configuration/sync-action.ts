"use server";

export async function syncCustomersServerAction() {
  'use server';
  return { success: false, message: 'Customer sync is no longer available. Manage customers directly via the Customers admin.' };
}
